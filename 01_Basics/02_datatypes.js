"use strict"; // treat all JS code as newer version

//alert(3 + 3); we are using nodejs, not browser

let name = "manas" //string
let age = 18 //integer
let isLoggedIn = false

// bigint datatype used for large numbers
// string=>""
// boolean => true false
// null => standalone value , state = null
// undefined => value not yet asssigned
// symbol => unique 

// object 

//  for string: integer values (“elements”) 
//  up to a maximum length of 2**53 - 1 elements

/*
    The Number type 
    has exactly 18,437,736,874,454,810,627
    (that is, 2**64 - 2**53 + 3) values
*/

console.log(typeof(null)); //object
console.log(typeof(undefined)); //undefined